# first step: create virtual enviroment 

1. `python -m venv venv`
2. `venv\Scripts\activate` (windows), `source venv/bin/activate` Mac or Linux
3. `pip install -r requirements.txt`

# second step: run the program

ACO contains the structured algorithm for finding the near-optimal solution, whereas hormigasmemoriacompartidanumba condenses everything into a single function using JIT compilation.

## how to execute the program?

`python aco.py`

`python hormigasmemoriacompartidanumba.py`
